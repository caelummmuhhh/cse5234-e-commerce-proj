import React from 'react';

const Purchase = () => {
    let TITLE = 'Purchase Page';

    return (
        <div>
            <h1>
                {TITLE}
            </h1>
        </div>
    );
};

export default Purchase;